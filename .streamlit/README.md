# Streamlit configuration

This folder contains the Streamlit configuration used by the public web version.

Files:

- `config.toml`: visual theme and safe server defaults.

Do not commit local secrets.

If secrets are needed later, create a local file named:

```text
.streamlit/secrets.toml
```

That file must remain ignored by Git.